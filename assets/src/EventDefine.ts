import { _decorator, Component, Node } from 'cc';
const { ccclass, property } = _decorator;

export enum EnumEventDefine{
    openResultView="openResultView",
}
@ccclass('EventDefine')
export class EventDefine extends Component {
    start() {

    }

    update(deltaTime: number) {
        
    }
}

